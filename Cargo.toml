[package]
name = "typenum-fractional"
version = "0.1.0"
edition = "2018"

categories = ["no-std"]

[dependencies]
typenum = { version = "~1.13.0", default-features = false }
const-default = { version = "^0.3.0" }

[features]
unstable = [ ]
